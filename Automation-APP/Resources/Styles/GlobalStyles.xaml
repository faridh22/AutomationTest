<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <!-- Primary application color -->
    <Color x:Key="PrimaryColor">#45A4F5</Color>

    <!-- Accent color used for notification badges -->
    <Color x:Key="NotificationBackgroundColor">#FF6F61</Color>

    <Style x:Key="PrimarySearchBar"
            TargetType="SearchBar">
        <Setter Property="BackgroundColor"
                Value="{DynamicResource PrimaryColor}"/>
    </Style>

    <Style x:Key="PrimaryLabel"
            TargetType="Label">
        <Setter Property="TextColor"
                Value="{DynamicResource PrimaryColor}"/>
    </Style>

    <!-- Style for the notification label displayed in the header -->
    <Style x:Key="NotificationLabel"
            TargetType="Label">
        <Setter Property="BackgroundColor"
                Value="{DynamicResource NotificationBackgroundColor}"/>
        <Setter Property="TextColor"
                Value="White"/>
        <Setter Property="Padding"
                Value="5"/>
    </Style>
</ResourceDictionary>
